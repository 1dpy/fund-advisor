# 依赖
node_modules/

# 项目级工作记忆 / 自动化配置(含本地敏感信息)
.workbuddy/

# 缓存与中间产物
cache/
bt_cache/
data_cache/
# 累积真实净值历史 + 自我迭代元参数(本地生成, 不入库; 每次clone重新seed)
data/
evolution.json
reports/
*.log

# 本地真实持仓(含个人隐私金额), 用 holdings.example.json 作模板
holdings.json
holdings.json.bak.*
data/uploads/

# 环境变量 / 密钥
.env
.env.local
.env.*.local
